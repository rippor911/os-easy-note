import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Boot 与启动","description":"","frontmatter":{},"headers":[],"relativePath":"OS Boot/2_0 Boot.md","filePath":"OS Boot/2_0 Boot.md","lastUpdated":1777274642000}');
const _sfc_main = { name: "OS Boot/2_0 Boot.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_QuizSet = resolveComponent("QuizSet");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="boot-与启动" tabindex="-1">Boot 与启动 <a class="header-anchor" href="#boot-与启动" aria-label="Permalink to &quot;Boot 与启动&quot;">​</a></h1><h2 id="一、boot-的核心矛盾" tabindex="-1">一、Boot 的核心矛盾 <a class="header-anchor" href="#一、boot-的核心矛盾" aria-label="Permalink to &quot;一、Boot 的核心矛盾&quot;">​</a></h2><p>Bootstrapping 简称 boot，直观含义是“自己把自己拉起来”。计算机启动确实有类似矛盾：</p><ul><li>必须先运行一段程序，计算机才能进入可用状态。</li><li>但程序又必须运行在已经被正确初始化的硬件环境上。</li></ul><p>因此，启动前硬件只能处在一个<strong>最安全、最通用、功能最弱</strong>的初始状态。启动过程的本质，是逐步设置硬件、加载更强的程序、交接控制权，最终把系统带到可以运行操作系统和用户程序的状态。</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>OS 启动可以理解为一个“逐步释放系统灵活性”的过程：从固定入口、固定地址和极少功能开始，逐步获得内存、缓存、文件系统、设备驱动和多任务能力。</p></div><h2 id="二、bootloader-的职责" tabindex="-1">二、Bootloader 的职责 <a class="header-anchor" href="#二、bootloader-的职责" aria-label="Permalink to &quot;二、Bootloader 的职责&quot;">​</a></h2><p>Bootloader 是操作系统内核运行之前的一段引导程序。它的名字可以拆成两层含义：</p><ul><li><strong>Booter：</strong> 初始化必要硬件，让系统至少部分运行起来。</li><li><strong>Loader：</strong> 把操作系统映像加载到内存，并跳转到内核入口。</li></ul><p>在嵌入式系统中，Bootloader 往往非常依赖具体硬件。即使 CPU 相同，不同开发板的 Flash、串口、内存控制器和外设也可能不同，所以 Bootloader 通常需要移植。MIPS 或其他嵌入式场景常见 U-Boot；传统 x86 Linux 场景常见 LILO 和 GRUB。</p><p>多数 Bootloader 可以分为两阶段：</p><table tabindex="0"><thead><tr><th>阶段</th><th>典型实现</th><th>主要职责</th></tr></thead><tbody><tr><td>stage1</td><td>汇编，强依赖 CPU 和板级硬件</td><td>固定入口启动、建立最小运行环境、初始化关键寄存器和早期内存</td></tr><tr><td>stage2</td><td>C 语言为主</td><td>提供较复杂功能，如串口输出、环境变量、存储设备、网络、文件系统、内核加载</td></tr></tbody></table><h2 id="三、mips-启动中的地址空间" tabindex="-1">三、MIPS 启动中的地址空间 <a class="header-anchor" href="#三、mips-启动中的地址空间" aria-label="Permalink to &quot;三、MIPS 启动中的地址空间&quot;">​</a></h2><p>MIPS 上电启动时，OS 尚未接管系统，TLB、Cache 和普通内存环境都不能假设已经可用。理解 MIPS 启动首先要看地址空间：</p><table tabindex="0"><thead><tr><th>区域</th><th>地址范围</th><th>特性</th><th>启动阶段意义</th></tr></thead><tbody><tr><td><code>kuseg</code></td><td><code>0x00000000</code> 到 <code>0x7fffffff</code></td><td>用户态可用，通常需要 MMU/TLB 转换</td><td>MMU 未设置前不能依赖</td></tr><tr><td><code>kseg0</code></td><td><code>0x80000000</code> 到 <code>0x9fffffff</code></td><td>内核态，直接映射低 512 MiB 物理地址，通常经过 Cache</td><td>Cache 未初始化前不能随意使用</td></tr><tr><td><code>kseg1</code></td><td><code>0xa0000000</code> 到 <code>0xbfffffff</code></td><td>内核态，直接映射低 512 MiB 物理地址，不经过 Cache</td><td>重启时最可靠的地址区域</td></tr><tr><td><code>kseg2/kseg3</code></td><td><code>0xc0000000</code> 到 <code>0xffffffff</code></td><td>内核态，通常需要 MMU/TLB 转换</td><td>MMU 未设置前不应访问</td></tr></tbody></table><p>MIPS 的启动入口地址是 <code>0xbfc00000</code>，它位于 <code>kseg1</code>。把高三位清零后，对应物理地址 <code>0x1fc00000</code>。选择这个区域的原因不是它更快，而是它在 TLB 和 Cache 都尚未准备好时仍能可靠工作。</p><h2 id="四、mips-u-boot-启动流程" tabindex="-1">四、MIPS / U-Boot 启动流程 <a class="header-anchor" href="#四、mips-u-boot-启动流程" aria-label="Permalink to &quot;四、MIPS / U-Boot 启动流程&quot;">​</a></h2><p>U-Boot 的早期启动通常从 <code>start.S</code> 的 <code>_start</code> 开始：</p><ol><li>初始化中断向量。</li><li>清零通用寄存器和部分协处理器寄存器。</li><li>配置 <code>CP0_STATUS</code>，设置协处理器、中断和 CPU 运行级别。</li><li>配置 <code>gp</code> 等基础寄存器。</li><li>进入 <code>lowlevel_init.S</code>，配置 CPU 主频、总线、DDR 等低层参数。</li><li>调用 cache 相关代码初始化 Cache。</li></ol><p>早期阶段有一个特殊问题：DDR RAM 尚未配置好，但调用 C 函数需要栈。课件中的处理方式是先把一部分 Cache 当作临时 RAM 使用，把它配置成栈并锁定。这样访问栈地址时只会访问 Cache，不会访问尚不可用的真实 RAM。</p><p>进入 C 阶段后，<code>board_init_f</code> 会做一系列板级初始化：</p><ul><li><code>timer_init</code>：时钟初始化。</li><li><code>env_init</code>：环境变量初始化。</li><li><code>init_baudrate</code>、<code>serial_init</code>：串口速率和串口初始化。</li><li><code>console_init_f</code>：控制台初始化。</li><li><code>display_banner</code>：显示 U-Boot 版本和启动信息。</li><li><code>init_func_ram</code>：初始化内存和 DDR 控制器。</li></ul><p>当串口和内存可用后，U-Boot 会划分内存、初始化堆栈、为自身代码预留空间，并把代码从 Flash 搬到 RAM 中继续执行。后续 <code>board_init_r</code> 初始化 Flash、PCI、网口等外设，最后进入命令行或直接启动 Linux kernel。</p><h2 id="五、linux-内核接管控制权" tabindex="-1">五、Linux 内核接管控制权 <a class="header-anchor" href="#五、linux-内核接管控制权" aria-label="Permalink to &quot;五、Linux 内核接管控制权&quot;">​</a></h2><p>Bootloader 的最终目标是把内核映像放到内存中的合适位置，然后跳转到内核入口。以 MIPS Linux 为例：</p><ul><li>Bootloader 将 Linux 内核映像拷贝到 RAM，并移动到指定物理地址。</li><li>内核入口位于体系结构相关的汇编代码中，例如 <code>/arch/mips/kernel/head.S</code> 的 <code>kernel_entry()</code>。</li><li>入口代码初始化内核栈，清零未初始化数据段，为创建第一个进程准备环境。</li><li>最后跳转到 <code>/init/main.c</code> 中的 <code>start_kernel()</code>。</li></ul><p><code>start_kernel()</code> 是 Linux 内核建立自身运行环境的主线，典型工作包括：</p><ul><li>设置 CPU ID，为多核环境做准备。</li><li>初始化内核核心数据结构。</li><li>关闭中断，避免初始化期间被异步事件打断。</li><li>初始化体系结构相关的内存、页表和命令行参数。</li><li>初始化调度器、定时器、软中断、时间系统和控制台。</li><li>初始化 <code>vmalloc</code>、伙伴系统、内存管理等模块。</li><li>在 <code>rest_init()</code> 中创建 <code>kernel_init</code> 和 <code>kthreadd</code>，并把当前执行流转为 idle 线程。</li><li>通过调度进入多任务运行状态，由 <code>kernel_init</code> 启动第一个用户进程。</li></ul><p>课程实验中的 MIPS 虚拟机和教学型操作系统比真实 Linux 简化很多，但边界一致：<strong>Bootloader 准备环境并跳到内核，内核再建立自己的运行时和第一个用户态入口</strong>。</p><h2 id="六、x86-bios-mbr-启动链路" tabindex="-1">六、x86 BIOS / MBR 启动链路 <a class="header-anchor" href="#六、x86-bios-mbr-启动链路" aria-label="Permalink to &quot;六、x86 BIOS / MBR 启动链路&quot;">​</a></h2><p>传统 x86 启动链路可以概括为：</p><ol><li>加电。</li><li>CPU 跳转到 BIOS 的固定物理地址，例如 Intel 80386 的 <code>0xffff0</code>。</li><li>BIOS 执行 POST 硬件自检。</li><li>BIOS 按启动顺序寻找可启动设备。</li><li>BIOS 从 MBR 加载启动扇区。</li><li>BIOS 把控制权交给 OS Bootloader。</li></ol><p>BIOS 存放在主板 ROM 或 EEPROM 中，提供最底层的硬件设置和控制。它会读取 CPU、内存、磁盘、时钟、PnP 和启动顺序等信息，并在硬件自检通过后继续启动。</p><p>BIOS 的典型限制包括 16 位实模式、寻址能力有限、结构和移植性较差，以及长期兼容历史硬件和软件的成本。UEFI 试图解决这些问题：</p><table tabindex="0"><thead><tr><th>对比项</th><th>BIOS</th><th>UEFI</th></tr></thead><tbody><tr><td>代码模式</td><td>传统 16 位实模式为主</td><td>可运行在 32 位或 64 位模式</td></tr><tr><td>结构</td><td>固化、整体性强</td><td>模块化，接口更清晰</td></tr><tr><td>分区支持</td><td>典型搭配 MBR</td><td>支持 GPT，可从超过 2 TiB 的磁盘引导</td></tr><tr><td>扩展能力</td><td>受历史兼容限制较多</td><td>支持更灵活的 pre-OS 环境和网络能力</td></tr></tbody></table><h2 id="七、mbr-与-grub" tabindex="-1">七、MBR 与 GRUB <a class="header-anchor" href="#七、mbr-与-grub" aria-label="Permalink to &quot;七、MBR 与 GRUB&quot;">​</a></h2><p>MBR 即 Master Boot Record，位于磁盘开头的特殊启动扇区，大小为 512 字节。它同时保存预启动代码和分区信息：</p><table tabindex="0"><thead><tr><th>区域</th><th>大小</th><th>内容</th></tr></thead><tbody><tr><td><code>0x000</code> 到 <code>0x1bd</code></td><td>446 字节</td><td>启动代码和数据</td></tr><tr><td><code>0x1be</code> 到 <code>0x1fd</code></td><td>64 字节</td><td>4 个分区表项，每项 16 字节</td></tr><tr><td><code>0x1fe</code> 到 <code>0x1ff</code></td><td>2 字节</td><td>启动签名 <code>0x55aa</code></td></tr></tbody></table><p>BIOS 读取 MBR 时会检查最后两个字节是否为 <code>0x55aa</code>。如果签名不存在，通常认为该介质不可启动。</p><p>MBR 分区表最多只有 4 个表项，因此传统分区模型中：</p><ul><li>主分区最多 4 个。</li><li>主分区和扩展分区总数不能超过 4 个。</li><li>扩展分区不能直接使用，必须再划分逻辑分区。</li><li>通常只有一个活动分区用于引导。</li></ul><p>GRUB 的磁盘引导过程也体现逐级加载：</p><table tabindex="0"><thead><tr><th>阶段</th><th>作用</th></tr></thead><tbody><tr><td>stage1</td><td>位于 MBR 的小段代码，负责加载下一阶段</td></tr><tr><td>stage1.5</td><td>识别文件系统，使 GRUB 能从文件系统读取后续文件</td></tr><tr><td>stage2</td><td>显示启动菜单，加载内核映像和可选的 <code>initrd</code></td></tr></tbody></table><p>BIOS 通常把硬盘主引导记录读入 <code>0x7c00</code>，检查 <code>0x7dfe</code> 处的启动签名，然后跳转到 <code>0x7c00</code> 执行。主引导程序再寻找活动分区，读取该分区的引导扇区，并继续交接控制权。</p><h2 id="八、linux-内核映像和用户态启动" tabindex="-1">八、Linux 内核映像和用户态启动 <a class="header-anchor" href="#八、linux-内核映像和用户态启动" aria-label="Permalink to &quot;八、Linux 内核映像和用户态启动&quot;">​</a></h2><p>传统 x86 Linux 课件脉络中，内核映像可以看作由引导代码、启动代码和压缩的内核主体组成。历史上常见：</p><ul><li><code>bootsect.S</code>：装载系统启动过程并设置启动参数。</li><li><code>setup.S</code>：初始化系统和硬件，切换到保护模式。</li><li><code>video.S</code>：初始化显示设备。</li><li><code>head.S</code>：进入 32 位内核早期入口，解压内核并跳到 <code>start_kernel()</code>。</li></ul><p>典型流程是：</p><ol><li><code>bootsect.S</code> 从 <code>0x7c00</code> 搬移自身，读取 setup 区，设置栈和磁盘参数。</li><li>根据内核大小，把内核映像加载到低端或高端内存。</li><li><code>setup.S</code> 检测物理内存布局，初始化部分硬件，打开 A20，建立 GDT/IDT，切换到保护模式。</li><li><code>head.S</code> 初始化段寄存器和临时栈，清零 BSS，解压内核。</li><li>解压后的内核建立临时页表和进程 0 的内核栈，跳转到 <code>start_kernel()</code>。</li><li><code>start_kernel()</code> 初始化调度、内存、中断、时间、slab 等内核模块。</li></ol><p>内核建立完成后，才进入用户层初始化。课件使用的是传统 SysV init 线索：</p><ul><li>内核启动第一个用户层程序 <code>/sbin/init</code>。</li><li><code>init</code> 读取 <code>/etc/inittab</code>，确定运行等级。</li><li>执行 <code>/etc/rc.d/rc.sysinit</code>，设置 <code>PATH</code>、网络、swap、<code>/proc</code> 等。</li><li>根据运行级别执行 <code>rc0.d</code> 到 <code>rc6.d</code> 中的脚本。</li><li>执行 <code>/etc/rc.d/rc.local</code> 中的用户自定义初始化。</li><li>执行 <code>/bin/login</code>，进入登录状态。</li></ul><div class="caution custom-block github-alert"><p class="custom-block-title">CAUTION</p><p>现代 Linux 发行版常用 <code>systemd</code> 替代传统 SysV init，但边界没有变：内核先完成自身初始化，然后启动第一个用户态 init 进程，由它继续拉起用户空间服务。</p></div><h2 id="九、易错点" tabindex="-1">九、易错点 <a class="header-anchor" href="#九、易错点" aria-label="Permalink to &quot;九、易错点&quot;">​</a></h2><ul><li>固件、Bootloader、内核不是同一层。固件负责最早期硬件发现和控制转移，Bootloader 负责准备内核运行环境，内核才是操作系统主体。</li><li>MBR 是整个磁盘的第一个扇区；Boot Sector 是某个分区的第一个扇区。</li><li>Bootloader 不负责长期管理资源，它只负责把系统带到“内核能接管”的状态。</li><li>MIPS 选择 <code>kseg1</code> 启动，是因为它不依赖 TLB 和 Cache，而不是因为它性能最好。</li><li>启动慢通常不是某一个步骤造成的，而是固件初始化、设备探测、内核初始化、模块加载和用户态服务启动共同叠加的结果。</li></ul><h2 id="十、交互式复习题" tabindex="-1">十、交互式复习题 <a class="header-anchor" href="#十、交互式复习题" aria-label="Permalink to &quot;十、交互式复习题&quot;">​</a></h2>`);
  _push(ssrRenderComponent(_component_QuizSet, {
    collection: "boot",
    title: "Boot 与启动练习",
    description: "覆盖 Bootloader、MIPS 启动、x86 BIOS/MBR、GRUB 和内核接管控制权。",
    "question-ids": [
      "boot-sequence-01",
      "boot-sequence-02",
      "boot-sequence-03",
      "boot-sequence-04",
      "boot-sequence-05",
      "boot-sequence-06",
      "boot-sequence-07",
      "boot-sequence-08"
    ]
  }, null, _parent));
  _push(`</div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("OS Boot/2_0 Boot.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _2_0_Boot = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _2_0_Boot as default
};
